name: Backend CI
on:
  push:
    paths: ['backend/**', 'shared/**']
  pull_request:
    paths: ['backend/**', 'shared/**']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Backend CI — lint, typecheck, and tests to be added"
