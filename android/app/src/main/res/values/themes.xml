<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base bridge theme — Compose handles the actual styling via CoinyTheme. -->
    <style name="Theme.Coiny" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
