-- SnapTrade removed. Its free tier permits ONE connected user for "testing and
-- personal use only", which a 30-person beta already breaches, and commercial
-- use is $1-2 per connected user per month against a total variable-cost budget
-- of $1.25. It also supplied only a single balance scalar, while Plaid
-- Investments already returns per-security holdings with cost basis for the same
-- accounts.
DROP TABLE IF EXISTS "snaptrade_connections";
