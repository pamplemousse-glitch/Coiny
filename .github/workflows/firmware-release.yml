name: Firmware Release
on:
  push:
    tags: ['fw/v*']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - name: Placeholder
        run: echo "Firmware release — PlatformIO build + OTA upload to be added"
