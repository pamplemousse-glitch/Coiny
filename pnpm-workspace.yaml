packages:
  - 'backend'
  - 'shared'
  - 'mobile'

onlyBuiltDependencies:
  - esbuild

# Native module pulled in by eslint-config-expo; needs its postinstall to build.
allowBuilds:
  unrs-resolver: true
